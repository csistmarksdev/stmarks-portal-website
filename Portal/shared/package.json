{
  "name": "@portal/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Types, DT shapes and constants shared by the Portal backend and admin frontend. Content types mirror the Website API contract exactly.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
